{
  "name": "postgres_Sink",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "test",
    "connection.url": "jdbc:postgresql://postgres:5432/exampledb",
    "connection.user": "docker",
    "connection.password":"docker",
    "insert.mode": "insert",
    "delete.enabled": "true",
    "table.name.format": "edge-to-central",
    "auto.create": "true",
    "auto.evolve": "true"
  }
}

